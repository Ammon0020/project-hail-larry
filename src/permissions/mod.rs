//! Permission request/response and policies (Go `internal/permissions/`).
//!
//! `allow_always` / `allow_session` policies, stale-prompt auto-deny, and an
//! audit log. Implementation lands in S-PERMISSIONS (Phase 2). S-ARCH scope:
//! module placeholder only.
