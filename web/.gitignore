# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
# Keep the empty directory available for rust-embed. Production Vite assets
# remain generated and ignored; `src/api/embed.rs` serves an embedded dev
# fallback until `npm run build` supplies `dist/index.html`.
!dist/
dist/*
!dist/.gitkeep
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
