- name: ChatComposer placeholder references @-mention feature that does not exist
- file: /media/adam/extex/projects/project-hail-larry/web/src/components/ChatComposer.tsx
- lines: 199
- description: The textarea placeholder reads "Type @ to bring in another conversation", but there is no `@`-mention / context-injection handler anywhere in ChatComposer or ChatPanel. There is no `onKeyDown`/`onChange` logic that detects `@`, no mention picker UI, and no call to any context-injection API. The `ContextInjectionBlock` component exists but is only rendered for events that already have `injectedContext` (ChatMessageItem line 141) — it is a display component, not an input trigger. A user who types `@` expecting a picker gets nothing, and the placeholder actively misleads them about a feature that isn't wired up. Either implement the `@`-trigger or change the placeholder to an accurate hint (e.g. "Type a message…").
- verification: Read ChatComposer lines 194-203 (the textarea) and lines 144-149 (handleKeyDown — only Enter/Shift check). Searched the file for `@` handling, mention, context injection — none found. ChatPanel's handleSend (lines 528-606) does not process `@` either. The `ContextInjectionBlock` is only a read-only display component (ContextInjectionBlock.tsx lines 13-35).
