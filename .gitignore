# Build artifacts
bin/
*.log
eslint-report.json

# Embedded frontend build output (generated by build.ps1)
internal/server/dist/

# Node
node_modules

# reviews
review/

# Rust build artifacts (Cargo.lock is committed — this is an application).
target/