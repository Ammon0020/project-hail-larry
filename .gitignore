# Build artifacts (anchored to repo root so it doesn't match src/bin/)
/bin/
*.log
eslint-report.json

# Node
node_modules

# reviews
review/

# Rust build artifacts (Cargo.lock is committed — this is an application).
target/

# Local rustup toolchain config (per-developer, not shared).
.rustup/

# Repomix packed output (regenerated on demand; never commit the artifact).
repomix-output*.xml