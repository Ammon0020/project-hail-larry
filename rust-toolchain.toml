# Pin the toolchain for reproducible builds across dev and CI.
# MSRV declared in Cargo.toml `rust-version`. S-ARCH acceptance criterion.
[toolchain]
channel = "1.92.0"
components = ["rustfmt", "clippy"]
profile = "minimal"
