# Repo-level Cargo config is intentionally minimal so CI runners without
# sccache or mold build out of the box. Local dev optimizations (sccache
# wrapper, mold linker) are written to the user-level ~/.cargo/config.toml by
# `scripts/setup.sh` — see that script and AGENTS.md for details.
